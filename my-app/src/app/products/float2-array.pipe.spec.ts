import { Float2ArrayPipe } from './float2-array.pipe';

describe('Float2ArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new Float2ArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
